=============================
``pot`` Digital Potentiometer
=============================

This is a tool for controlling digital potentiometers: device info, wiper
set/get, relative move, shutdown control and non-volatile store/recall.
Run ``pot`` without arguments for the command list. The device path defaults
to ``CONFIG_EXAMPLES_POT_DEVPATH``.
